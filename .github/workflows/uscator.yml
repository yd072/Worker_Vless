name: Obfuscate and Commit

on:
  workflow_dispatch:

jobs:
  obfuscate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: Install dependencies
        run: npm install -g javascript-obfuscator

      # 确认文件是否存在
      - name: List files to confirm 节点订阅.js exists
        run: ls -al

      # 简化混淆命令用于调试
      - name: Obfuscate code (simplified for debugging)
        run: |
          javascript-obfuscator 节点订阅.js --output _worker.js || echo "Obfuscation failed"

      # 查看 Git 状态，确认是否有更改
      - name: Check git status
        run: git status

      # 提交变更
      - name: Commit changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add _worker.js || echo "No files to add"
          git commit -m "Obfuscate _worker.js" || echo "No changes to commit"

      # 推送更改
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref }}
